# Bugs

After some time (~10 minutes) the shell crashes.
However it not only crashes, but takes all GTK applications with it.
ps aux show 'niri session shutdown target' or something.

restart shell: settings like accent color or other gnome settings are just gone.

Something very wrong. Maybe dbus issue?

