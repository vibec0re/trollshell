[package]
name = "hytte-reactive"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "GTK4 ↔ futures-signals bridge and service registry for hytte"

[lints]
workspace = true

[dependencies]
tokio = { version = "1.52.1", features = ["rt-multi-thread", "sync", "net", "io-util", "time", "macros"] }
